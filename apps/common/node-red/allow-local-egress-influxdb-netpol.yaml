apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "service-rule"
spec:
  endpointSelector:
    matchLabels:
      app: node-red
  egress:
  - toServices:
    - k8sService:
        serviceName: influxdb
        namespace: influxdb