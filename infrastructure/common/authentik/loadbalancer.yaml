apiVersion: v1
kind: Service
metadata:
  name: authentik-public
  namespace: authentik
spec:
  selector:
    app: authentik
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
