apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-secret-cryptoautomation
  namespace: crypto-automate
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: secret-cryptoautomate # secret-cryptoautomation
  dataFrom:
  - extract:
      key: secret/cryptoautomation
