apiVersion: v1
kind: Service
metadata:
  name: mongodb-lb
  namespace: mongodb
  labels:
    app: mongodb
spec:
  type: LoadBalancer
  ports:
    - name: db
      port: 27017
      protocol: TCP
  selector:
    app: mongodb
