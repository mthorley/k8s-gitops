apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-secret-cryptoautomate
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: secret-cryptoautomate
  dataFrom:
  - extract:
      key: secret/cryptoautomation
