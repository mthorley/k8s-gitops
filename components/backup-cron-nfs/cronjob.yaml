apiVersion: batch/v1
kind: CronJob
metadata:
  name: "${APP}-backup"
spec:
  schedule: "${BACKUP_SCHEDULE}"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: "${APP}-backup"
            image: alpine:3
            imagePullPolicy: IfNotPresent
            command:
            - /bin/sh
            - -c
            - |
              set -e
              DATE="$(date +%Y%m%d_%H%M%S)"
              tar czf "/backup/${APP}_$DATE.tar.gz" --exclude='node_modules' --exclude='.npm' -C /source .
              cd /backup && ls -1t ${APP}_*.tar.gz | tail -n +11 | xargs -r rm -f
            volumeMounts:
              - name: source
                mountPath: /source
                readOnly: true
              - name: nfs-volume
                mountPath: /backup
          restartPolicy: OnFailure
          volumes:
            - name: source
              persistentVolumeClaim:
                claimName: "${BACKUP_SOURCE_CLAIM}"
            - name: nfs-volume
              nfs:
                server: "${qnap_ip}"
                path: "/k8s/backup/${cluster_id}/${APP}"
