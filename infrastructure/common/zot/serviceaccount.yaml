# Vault auth identity for the secrets-eso-vault component (bound to
# ${APP}-secrets-role in setup/vault/kv2.tf). It exists so the
# letsencrypt component can read the Cloudflare DNS token; the zot pod runs
# as the chart's own ServiceAccount.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${APP}
