apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nginx-gateway
  namespace: flux-system
spec:
  interval: 30m0s
  dependsOn:
    - name: infrastructure
    - name: gateway-api-crds
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/common/nginx-gateway
  prune: true
  postBuild:
    substitute:
      domain: cluster1.cyonomy.net
