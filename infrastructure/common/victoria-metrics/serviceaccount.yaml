# Vault auth identity for the secrets-eso-vault component (bound to
# victoria-metrics-secrets-role in setup/vault/kv2.tf). Not the SA the
# vmsingle pod runs as - the chart creates its own.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: victoria-metrics
