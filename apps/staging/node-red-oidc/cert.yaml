apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: internal-com
  namespace: node-red-oidc
spec:
  dnsNames:
    - noderedoidc.internal.com
  secretName: internal-com-tls
  issuerRef:
    name: vault-issuer
