apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-metrics
spec:
  chart:
    spec:
      chart: victoria-metrics-single
      sourceRef:
        kind: HelmRepository
        name: victoria-metrics
      version: "0.46.0"
  interval: 1h0m0s
  values:
    server:
      # VM rejects samples older than this at ingest, so it must cover the
      # oldest InfluxDB history being migrated (mid-2022).
      retentionPeriod: 5y
      extraArgs:
        # de-duplicate samples with identical timestamps, so re-running the
        # influx migration (or dual-writing) never double counts
        dedup.minScrapeInterval: 1ms
      persistentVolume:
        enabled: true
        storageClassName: managed-nfs-storage
        size: 20Gi
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          # 1Gi was OOMKilled during bulk vmctl imports
          memory: 2Gi
      # scraped by the prometheus-operator in monitoring ns
      serviceMonitor:
        enabled: true
      # exposed via envoy-gateway, see gateway.yaml
      ingress:
        enabled: false
